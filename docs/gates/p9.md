# Gate P9 — value tracking (time-saved / ROI metrics)

**Verdict: PASS**

- **Phase:** P9 — value tracking: time-saved metrics per run, all-up ROI summary, customer-facing report
- **HEAD commit:** c78e1882f015ab2bb47e7e0cf66c2535b095dea9
- **Verified by:** verifier (independent; did not write the code)
- **Date:** 2026-06-22

## Done when (docs/agntic-ops-gap-and-build-plan.md:319-321)
> a workflow run records execution time; the profile page renders baseline, execution
> time, time-saved YTD, trend, example outputs, and baseline recording; the page is
> accessible from the console workflow detail view.

(Gate script `scripts/gates/p9.sh` encodes: (1) value-calc config with
`computeTimeSavedMinutes`; (2) `time_saved_minutes` column live in workflow_runs;
(3) per-workflow profile endpoint computing time-saved; (4) `GET /api/console/roi`
returning `totalTimeSavedMinutes`; (5) profile surface in UI.)

## Gate script output
```
── gate: running Phase 9 check (scripts/gates/p9.sh) ──
p9 PASS: value config defined; time_saved_minutes column live; profile + ROI endpoints present; UI surface present
── gate: Phase 9 check PASSED ──
EXIT_CODE: 0
```

## Evidence (independently reviewed, not just script-trusted)

1. **Value methodology config** — `src/value/time-saved-config.js:21-24` exports
   `computeTimeSavedMinutes(baselineDurationS, actualDurationS)` =
   `max(0, (baseline - actual)/60)`, returns null when no baseline. `METHODOLOGY = 'baseline_delta'`, `UNIT = 'minutes'`.

2. **time_saved_minutes column live** — `sqlite3 ./memory/workflows/workflows.sqlite
   "SELECT name,type FROM pragma_table_info('workflow_runs') WHERE name='time_saved_minutes'"`
   → `time_saved_minutes|REAL`.

3. **Engine populates it** — `src/workflows/workflow-scheduler.js:275` computes
   `actualDurationS`; `:290-291` derives `timeSavedMinutes` from `workflow.baseline_duration_s`;
   `:292` passes it to `completeRun`. `src/workflows/workflow-store.js:621,633-636`
   persists it via `UPDATE workflow_runs SET time_saved_minutes = ?`.

4. **Per-workflow profile endpoint** — `src/api/console.js:163`
   `GET /api/console/workflows/:id/profile` returns baseline (durationS,
   recordingPath, setAt — :205-209), `timeSavedYtdS` (:177-182,210), `lastRunMs`
   (:184,211), `sparkline` trend (:186-189,212), `recentOutputs` example outputs
   (:191-201,213). Tenant-scoped (:166).

5. **All-up ROI endpoint** — `src/api/console.js:278` `GET /api/console/roi` returns
   `totalTimeSavedMinutes` (:314), `totalRuns`, `byWorkflow`, `generatedAt`. Uses
   stored `time_saved_minutes` with pre-P9 backfill (:292-298).

6. **UI profile surface in console detail view** — `public/index.html:913` Profile
   tab (`onSwitchProfile`) alongside the dashboard view; `:1009-1017` renders
   time-saved YTD / last-run / baseline; `:1023,1026` recording + trend chart
   containers; `:1029-1046` recent outputs; `:1061-1115` baseline-capture panel
   (screen recorder + duration). `switchConsoleView`/`_loadConsoleProfile`
   (:2512-2531) fetch `/api/console/workflows/:id/profile`; `_renderProfileChart`
   draws the trend, `_renderProfileRecording` plays the baseline recording.

7. **Server wiring + syntax** — `src/api/server.js:75` mounts console routes;
   `:614` serves `/recordings` static. `node --check` passes for console.js,
   time-saved-config.js, and workflow-scheduler.js.
