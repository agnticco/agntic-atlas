# Gate P8 — Web research (native search_web) + filesystem connector

**Verdict: PASS**

- **Phase:** P8 — web research + filesystem connectors
- **HEAD commit:** 807c618787455456d9f9dee40a6372b6c3a252f4
- **Verified by:** verifier (independent; did not write the code)
- **Date:** 2026-06-21

## Done-when (as governed by the recorded closed decision)

The original build-plan prose (docs/agntic-ops-gap-and-build-plan.md:274-287)
described a `web_research` connector with Tavily search + Firecrawl fetch at
`src/connectors/web-research.js`. That scope was **re-decided and recorded** in
CLAUDE.md:185-188 (and reflected in the Phase status line, build plan:304):

> P8 — web research (`search_web` native Anthropic tool, already built) +
> filesystem connector (`filesystem_read`/`filesystem_list`, tenant-sandboxed to
> Knowledge-page approved folders)

The effective Done-when (encoded in scripts/gates/p8.sh:4-15):
1. `search_web` node type exists, registered in `registerBuiltInNodeTypes`, using
   Anthropic's native `web_search_20260209` tool.
2. Filesystem connector at `src/connectors/filesystem.js` with `filesystem_read`
   + `filesystem_list` capabilities.
3. Both capabilities registered in server.js via `registerFilesystemCapabilities`.
4. `injectFilesystemContext()` called in all run paths (REST `/workflows/run`,
   scheduler token injector, Slack + Airtable event dispatch).
5. Sandbox: `findApprovedRoot` rejects paths outside tenant-approved folders.
6. End-to-end: a `filesystem_read` node returns file content in workflow output.

## Gate script output

```
$ bash scripts/gate.sh 8
── gate: running Phase 8 check (scripts/gates/p8.sh) ──
p8: running gate checks...
  [ok] search-web.js exists
  [ok] searchWebNodeType exported
  [ok] search_web registered in registerBuiltInNodeTypes
  [ok] search_web uses Anthropic native web_search tool
  [ok] filesystem.js exists
  [ok] registerFilesystemCapabilities exported
  [ok] filesystem_read capability defined
  [ok] filesystem_list capability defined
  [ok] sandbox guard: findApprovedRoot exists
  [ok] FILESYSTEM_CAPABILITY_IDS exported
  [ok] registerFilesystemCapabilities imported in server.js
  [ok] registerFilesystemCapabilities called in server.js
  [ok] injectFilesystemContext defined
  [ok] injectFilesystemContext called in scheduler token injector
  [ok] injectFilesystemContext called in REST /workflows/run
  [ok] injectFilesystemContext called in event dispatch
  [ok] guard() rejects unapproved paths
  [ok] filesystem_read end-to-end: reads a file within an approved folder
  [ok] filesystem.js parses cleanly
  [ok] server.js loads cleanly

p8: 20 passed, 0 failed
p8: GATE PASSED
── gate: Phase 8 check PASSED ──
(exit 0)
```

## Independent evidence reviewed (beyond the grep checks)

- **End-to-end filesystem read (re-run directly):**
  `node scripts/checks/p8-filesystem-read.mjs` →
  `P8-FS-PASS: /var/folders/.../atlas-p8-WiGKtI/project-folder/README.md` (exit 0).
  The fixture content ("P8 filesystem gate fixture") flowed into the
  `filesystem_read` node output via `{{nodeId.output}}`, proving real file read,
  not just code presence. (scripts/checks/p8-filesystem-read.mjs:52-63)

- **Four run paths wired** (all call `injectFilesystemContext`):
  - Slack event dispatch — src/api/server.js:281
  - Airtable event dispatch — src/api/server.js:323
  - Scheduler token injector — src/api/server.js:491
  - REST `/workflows/run` — src/api/server.js:1088
  `injectFilesystemContext` definition — src/api/server.js:235-243 (stamps
  `_tenantId` only into filesystem connector-action nodes).

- **Capability registration** — src/api/server.js:530 calls
  `registerFilesystemCapabilities(engine.capabilityRegistry, ...)`; import at
  src/api/server.js:72.

- **Sandbox is real, not nominal** — `findApprovedRoot`
  (src/connectors/filesystem.js:17-25) resolves the requested path and matches it
  against tenant approved roots only; `guard()` (lines 28-39) throws "not within
  any approved folder" otherwise. Gate's adversarial check (`/etc/passwd` with
  empty approved folders) was rejected as designed (scripts/gates/p8.sh:85-95).

- **search_web native tool** — `searchWebNodeType` (src/workflows/node-types/search-web.js:30)
  uses `web_search_20260209` (line 18) and errors clearly on non-Anthropic
  providers (lines 60-65); registered in
  src/workflows/node-types/index.js:28.

## Residual (non-blocking, documentation only)

The Phase 8 prose body in docs/agntic-ops-gap-and-build-plan.md:274-287 still
describes the superseded web_research/Tavily/Firecrawl design (including a
Firecrawl "fetch" mode and `src/connectors/web-research.js`, which do not exist).
The governing decision (CLAUDE.md:185-188, build plan:304) supersedes that prose.
Recommend the Builder update lines 274-287 to match the closed decision. This is
a stale-doc residual, not a gate failure.
