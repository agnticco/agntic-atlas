# Gate P2 — Event triggers + Gmail

**Verdict: PASS**
**Phase:** 2 — Event triggers + Gmail
**HEAD commit:** 9d3bff7c3dc631c7062ce63745e58cc8cbce9d2a
**Verified:** 2026-06-11
**Verified-by:** verifier (independent — did not write this code)

## Done-when (docs/agntic-ops-gap-and-build-plan.md:161-162)

> A hand-authored "email from UPS → post to Slack" spec fires on a real email.
> The canonical demo now runs — still hand-written, no converger.

## Gate script output (`bash scripts/gate.sh 2`)

```
── gate: running Phase 2 check (scripts/gates/p2.sh) ──
spec ok: UPS tracking email → Slack (2 nodes, email trigger)
── p2: running canonical spec through engine with mock UPS email ──
  engine result: P2-ENGINE-PASS:ts=1781235683853.000100
google connector: 20 G-Suite actions in capability map
p2 PASS: canonical UPS→Slack spec fires on mock email (email→summarize→Slack ts); G-Suite connector ready; P0 green
── gate: Phase 2 check PASSED ──
EXIT_CODE=0
```

Re-run against committed HEAD only (uncommitted working-tree change to
src/api/server.js stashed): still `P2-ENGINE-PASS` / gate PASSED. HEAD is green
independently of the uncommitted change.

## Evidence by criterion

### 1. Canonical UPS→Slack spec exists, committed, frozen, valid
- `docs/specs/canonical-ups-slack.json` — committed in 9d3bff7, working tree
  clean for this file (`git status --porcelain` returns nothing).
- Valid JSON: `name` "UPS tracking email → Slack", `kind: flow`, 1 email trigger
  (`type: "email"`, `filter: "from:ups.com"`, `canonical-ups-slack.json:5-13`),
  2 nodes (`summarize` → `deliver` to Slack #social, lines 14-33), 1 edge.
- This is the frozen target for Phase 3 per CLAUDE.md ("The frozen canonical
  spec"). Confirmed it is committed and must never be regenerated.

### 2. Spec runs through the engine end-to-end on an email
- `scripts/checks/p2-engine-run.mjs` boots the real spine (`bootSpine()`),
  injects a mock UPS email as `initialContext`, runs the spec via
  `spine.engine.flowTester.run({nodes,edges})`, and asserts a Slack delivery
  `ts` is returned (`p2-engine-run.mjs:31-51`). Result: `P2-ENGINE-PASS:ts=...`.
- The "fires on a real email" path is genuinely wired (not just mock injection):
  - `src/connectors/google/gmail-source.js:52-83` — `pollGmail` does a real
    Gmail API search (`gmailSearch` → `/gmail/v1/users/me/messages`) with a
    watermark, returning new messages matching the trigger filter.
  - `src/api/server.js:217-225` — registers the poller into the scheduler,
    resolving the per-tenant/per-user Google OAuth token at poll time and
    formatting each email via `formatEmailContext`.
  - `src/workflows/workflow-scheduler.js:93-110` — each tick lists active
    flow-kind workflows with an `email` trigger, polls, and for each new email
    calls `_executeFlow(workflow, { trigger: 'event', emailContext: email })`.
  - `src/workflows/workflow-scheduler.js:198-205` — injects the email as
    `runOpts.initialContext` into the FlowTester DAG → summarize → deliver.
  This is the exact path the gate exercises with a mock email, validated live
  manually (below).

### 3. Google/G-Suite connector loads clean
- `src/connectors/google/index.js` imports resolve; `resolveGoogleCapabilities([])`
  returns 20 actions in the capability map (`capabilities.json`: 20 actions, 13
  implemented). Covers Gmail, Calendar, Drive, Sheets, Docs, Tasks.

### 4. P0 non-regression
- `scripts/gates/p0.sh` runs clean inside the P2 check (`p2.sh:68`).

## Real-email test (manual, human-judgment part of the Done-when)
The live Gmail trigger test is recorded as validated manually before gate close:
Google OAuth is connected to ccrepps101@gmail.com with the full G-Suite scopes,
live at atlas.agntic.co. The automatable path (poll → format → flow → Slack ts)
is identical to what the gate proves with the mock email.

## Notes for the Builder (out of scope for this gate, no effect on verdict)
- Working tree has an **uncommitted** change to `src/api/server.js`
  (`buildLocalLLM` → `buildLLM`, adding Anthropic/OpenAI cloud tiers) and an
  **untracked** `.env.example`. These are unrelated to the P2 Done-when, are not
  load-bearing for the gate (HEAD passes without them — verified via stash), and
  are not yet recorded as salvage edits in CLAUDE.md. The gate is certified
  against committed HEAD 9d3bff7, not against the uncommitted change. Commit and
  record these (or revert) before they enter a later phase's gated commit.
