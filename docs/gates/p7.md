# Gate P7

Date: 2026-06-20
HEAD: 502ef7e3d38c00b38aa71e219dad8bf050aa3c1c
Verified-by: verifier

## Done-when (docs/agntic-ops-gap-and-build-plan.md:231-232)
"all three of Maggie's connectors are live, failures retry and notify, and
sub-daily schedules fire correctly."

## Objective check
`bash scripts/gate.sh 7` → 30 passed, 0 failed, GATE PASSED (exit 0).

## Evidence-backed review

### (a) Unified CapabilityRegistry catalog
- `src/connectors/capability-registry.js:18` — `export class CapabilityRegistry`
  with `register` (39), `list({position,connector})` (66), `get(id)` (77),
  position-aware `availability`/`isReady` probe (87-101).
- `src/api/server.js:315` instantiates `new CapabilityRegistry()`;
  `:317` `new ChannelRegistry(capabilityRegistry)`; registrars wired at 320-322.

### (b)(c) Airtable connector live
- `src/connectors/airtable/index.js:57` synthetic owner `wsinstall:${tenantId}`;
  `storeAirtableToken` (59), CRUD handlers: list (142), get (151 via 156),
  search (156), create (159), update (165), delete (171).
- `initWebhookStore` (92) persists to `./memory/airtable-webhooks.json` (14).
- `verifyAirtableSignature` (129) HMAC-SHA256 over rawBody, timing-safe compare,
  reads `x-airtable-content-mac`.
- `registerAirtableChannels` (211) registers 6 CRUD step/delivery capabilities
  (224-302) + `airtable_record_changed` trigger `positions:['trigger']` no handle
  (305-314). Called in server.js:322.

### (d) Sub-daily scheduling
- `src/workflows/workflow-store.js:850` `_isFlowDue`: `*/N * * * *` every-N-min
  (865-872) and `0 */N * * *` every-N-hour (875-882), both elapsed-time dedup
  via `Date.now() - last_run >= intervalMs` (870, 880) — distinct from the
  calendar-day path for daily/weekly (912-914).

### (e)(f) Retry + notify
- `src/workflows/workflow-scheduler.js:208` `_executeFlow` reads
  `error_handling.retry.attempts`/`delay_seconds` (210-211), loops
  `maxAttempts` calling `_runFlowOnce` (214-221), then `_errorNotifier` (223-225).
- `registerErrorNotifier` (147). Wired in `src/api/server.js:447` — resolves the
  tenant Slack bot token (456) with `SLACK_BOT_TOKEN` fallback (459), posts to
  `chat.postMessage` on `error_handling.notify.{type:'slack',channel}` (448-466).

### (g) Google write-back
- `src/connectors/google/index.js`: `gmail_send` (384), `calendar_create_event`
  (424), `sheets_append` (466), `docs_create` (492), `tasks_create` (518) all
  `positions:['step','delivery']`; `gmail_new_message` trigger (534).
  `registerGoogleChannels` called in server.js:321.

### Modules load (grep passes don't prove parse)
- `import('./src/workflows/workflow-store.js')` → STORE_OK
- `import('./src/api/server.js')` → SERVER_OK
- `import('./src/connectors/airtable/index.js')` → AIRTABLE_OK

## Verdict
PASS
