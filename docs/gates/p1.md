# Gate P1 — PASS

- **Date:** 2026-06-09
- **Verified commit:** e6a4548fe67f93912da8f3d2d9976ae1e6ee33c3 (`feat/p1-slack`, tree clean)
- **Verifier:** independent (did not write the code)
- **Verdict:** PASS

## Done-when (docs/agntic-ops-gap-and-build-plan.md:155)
"clicking 'run' posts to Slack" — a hand-authored one-step spec runs through the
engine and a message lands in Slack (chat.postMessage returns a ts).

## Objective check
Command: `bash scripts/gate.sh 1` → exit 0.
```
capability: slack [available] config=target,body
run test-... -> posted to #atlas-test, ts=1718000000.000100 (stub)
stub received: channel=#atlas-test text="Hello from Atlas — P1 spine proof. If you can re…"
p1 PASS: hand-authored spec ran through the engine and posted to Slack (chat.postMessage returned a ts)
```
`scripts/gates/p1.sh` is the real check (not the fail-closed stub): it boots the
real spine, POSTs the spec to /workflows/run, and asserts a delivery `ts` plus (in
stub mode) that the stub received the spec's channel + text.

## Criteria + evidence
1. **Connector built fresh + registered (not dead code).**
   `src/connectors/slack/index.js:42` `registerSlackChannel`; called in the engine
   build at `src/api/server.js:75`, registry passed to FlowTester
   (`src/api/server.js:89`) and returned engine (`:98`).
2. **Hand-authored one-step spec, proprietary shape.**
   `docs/specs/p1-slack-hello.json` — valid JSON (gate's `node JSON.parse` passes);
   single `deliver` node, `config.channel="slack"` (lines 6-15). Not the frozen
   canonical (that is P2 UPS→Slack).
3. **Click run → engine → Slack with a ts.**
   `POST /workflows/run` (`src/api/server.js:229`) runs spec via FlowTester
   (`:237`), surfaces deliveries (`:250`). Connector genuinely POSTs to
   `${apiBase}/chat.postMessage` (`src/connectors/slack/index.js:64`) and returns
   `data.ts` (`:75`). Check returned ts in deliveries[0].
4. **Capability schema exposed.** `GET /capabilities` (`src/api/server.js:223`)
   → channelRegistry.getAll(); check saw `capability: slack [available]`.

## Anti-gaming — independent reproduction
- `node scripts/checks/slack-post.mjs` → SLACK-POST-PASS, exit 0.
- **Independent E2E (verifier's own stub + own spec, novel text/channel):**
  - Unique text: `Independent verifier probe VERIFIER-NONCE-9liwc3uy6ho-1780982511531 — only my code produced this.`
  - Unique channel: `#verif-511531`; stub's unique ts: `9999.1780982511531`
  - Result: stub received MY exact text + channel; /workflows/run response carried
    MY stub's ts. A hardcoded path cannot echo novel text → real HTTP POST confirmed.
- **Failure path demonstrated:** stub returning `{ok:false, error:"channel_not_found"}`
  → connector throws (`src/connectors/slack/index.js:72-73`) → run fails HTTP 502,
  `completed:false`. No silent success.

## Non-regression
`bash scripts/gates/p0.sh` → exit 0 (engine+auth boot, /health 200, server.js clean UTF-8).

## Record honesty
Channel-not-MCP decision recorded at `docs/connectors/slack.md:6`. Commit
`git show --stat e6a4548`: touches only src/api/server.js (+38) and new connector/
spec/check/doc files — no MCP runtime ported, matching the commit body.
