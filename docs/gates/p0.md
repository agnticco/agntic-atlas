# Gate P0 — clean spine

**Verdict: PASS**

- **Date:** 2026-06-08
- **Phase:** P0 — New repo, clean spine
- **Verified commit:** `7fc1f5015ac4431cad17d31c2c121c04106fbfad` (`7fc1f50`, branch `feat/p0-spine`)
- **Verified by:** Verifier (independent; did not author the spine)
- **Tree state at verification:** `git status --porcelain` clean (no output, exit 0)

## Done-when (authoritative)

`docs/agntic-ops-gap-and-build-plan.md:148-149` — "the existing engine boots in
the new repo and the empty UI reaches it over one health route." Operationalized
in `scripts/gates/p0.sh`: (1) engine + auth boot without error, (2) `GET /health`
-> 200 with `status/engine/auth` all `"ok"`, (3) `src/api/server.js` is clean
UTF-8 with no NUL byte and plain `grep` is not defeated.

## Objective check

Command (run from repo root):

    bash scripts/gate.sh 0

Output:

    ── gate: running Phase 0 check (scripts/gates/p0.sh) ──
    p0 PASS: engine+auth boot; GET /health -> 200 {"status":"ok","uptime":0,"version":"0.1.0","engine":"ok","auth":"ok"}; src/api/server.js is clean UTF-8 (grep ok, no NUL)
    ── gate: Phase 0 check PASSED ──

Exit code: 0.

The committed gate script (`git show HEAD:scripts/gates/p0.sh`) is the real,
ungamed check — it boots the actual server, polls `/health`, asserts the 200 +
payload, and runs both a perl NUL scan and a behavioral plain-vs-`grep -a`
count assertion. It is `set -eu` fail-closed. Not a stub.

## Criteria + evidence

1. **Minimal server, not a salvage bulk-port.** `src/api/server.js` is 115 lines
   (full read), mounts only express/cors/cookie-parser + `createAuthSubsystem`
   (auth) + `WorkflowStore`/`FlowTester`/`WorkflowService` (engine), exposes one
   route `GET /health` (`src/api/server.js:78`). Header comment
   (`src/api/server.js:4-15`) documents the deliberate omission of RAG, llama.cpp
   inference, model pool, MCP runtime.

2. **Clean UTF-8, no NUL, grep not defeated.**
   - `perl -0777 ... index($_,"\x00")` -> NO NUL.
   - `file src/api/server.js` -> "Unicode text, UTF-8 text".
   - `grep -c 'express'` = 4; `grep -ac 'express'` = 4 (equal -> plain grep works).

3. **Engine + auth genuinely boot (not stubbed).**
   - `GET /health` -> HTTP 200 `{"status":"ok","uptime":0,"version":"0.1.0","engine":"ok","auth":"ok"}` (independent boot on port 4123, not just the gate script).
   - Engine: `WorkflowStore` uses `better-sqlite3` and runs real `CREATE TABLE`
     schema init (`src/workflows/workflow-store.js:16,21,45,60,97`).
   - Auth/vault: `createAuthSubsystem` (`src/auth/index.js:11,59`), better-sqlite3
     stores (`session-store.js`, `oauth-token-store.js`, `user-store.js`), argon2
     (`local-auth-provider.js:4,43`), AES-256-GCM (`src/auth/token-cipher.js:17`),
     JWT sessions. On boot the live auth subsystem printed a real one-time admin
     setup token — proof the user/credential store is operating, not stubbed.

4. **Source closure present + tracked; deferred subsystems NOT bulk-ported.**
   - Present & git-tracked: `src/core` (4), `src/utils` (6), `src/graph` (16),
     `src/workflows` (29), `src/auth` (12), `src/llm/native-citations.js`,
     `src/connectors/connector-manifest.js`.
   - Absent (correctly deferred): `src/rag`, `src/llama`, `src/mcp`,
     `src/agent-graph`. The only `llama`/`rag` token matches in tracked src are
     comments in `server.js:5,77` describing what is intentionally not ported.
   - `git ls-files src/api/server.js package.json package-lock.json` shows all
     three tracked.

5. **Gate script ungamed.** Reviewed full `scripts/gates/p0.sh`; fail-closed,
   boots the real server, no shortcuts.

## Adversarial spot-checks

- **Independent boot:** booted `node src/api/server.js` myself on PORT=4123 with a
  fresh temp DB — `/health` returned 200 with the sane payload above; stdout showed
  "atlas spine listening on :4123 (engine ok, auth ok)" plus a real admin token.
- **NUL-regression catch (on a COPY, real file never modified):** injected a NUL
  into a temp copy of server.js. perl assertion exited 1 (= NUL detected = gate
  fails); plain `grep -c` errored/non-numeric while `grep -ac` = 4, so the
  script's `plain_count != forced_count` branch fails-closed. The encoding
  assertions would catch the agntic-prod gotcha recurring. Real file re-checked
  after: no NUL.
- **No prior claim:** `git log --all --grep "^Gate:"` returns nothing — P0 was
  not already claimed closed. No pre-existing `docs/gates/p0.md`.
