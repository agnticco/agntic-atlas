# Gate P11 — E2E validation + production hardening + VPS migration

**Verdict: PASS**

- **Phase:** P11 — E2E validation + production hardening + VPS migration
- **HEAD commit:** cebe441fc6e3bd99433ea3121cd04244272925cb
- **P11 work landed in:** `d73b813` (Tier 1 crash-safety) … `75891b7` (Tier 5 scale-later
  seams) + `2106f71` `feat(p11): gate artifacts — E2E, adversarial isolation, smoke, runbook`
  — all confirmed ancestors of `origin/main` (`git merge-base --is-ancestor`).
- **Verified by:** verifier (independent; did not write the code)
- **Date:** 2026-07-13

## Done when (scripts/gates/p11.sh:4-10)
> 1. E2E suite covers the full journey: builder (converger) → run → console
>    (inventory + monitoring + SOP export) → value/ROI summary — and PASSES.
> 2. Cross-tenant isolation proven adversarially: tenant A cannot read, list,
>    or trigger any resource belonging to tenant B (workflows/runs/vault/RAG).
> 3. VPS deployment runbook exists and is followable.
> 4. DNS points at the VPS and production smoke tests pass.

The gate was un-closeable until production existed: `scripts/gates/p11.sh:37-39`
fail-closes when `PROD_HOST` is unset. Atlas is now live at https://atlas.agntic.co
(AWS Lightsail `YOUR_SERVER_IP`, behind a Cloudflare Tunnel), so step 4 can run.

## Gate script output
```
$ PROD_HOST=atlas.agntic.co bash scripts/gate.sh 11
── gate: running Phase 11 check (scripts/gates/p11.sh) ──
p11: running E2E full-journey suite...
p11: running cross-tenant adversarial check...
p11: running production smoke against atlas.agntic.co...
p11 PASS: E2E green; cross-tenant isolation adversarially confirmed; runbook present; DNS live; smoke passed
── gate: Phase 11 check PASSED ──
EXIT_CODE=0
```
Run twice, including once through the **pristine committed** `scripts/gate.sh` (a
parallel session had an unrelated working-tree edit widening the phase argument to
`0-12`; it was stashed for the authoritative run and restored afterward). All five
P11 artifacts were clean vs `HEAD` and identical to `origin/main` at run time —
the gate passed against committed code, not local edits.

The check is substantive, not vacuous: it runs a real 7-test E2E suite against a
booted spine, a real 18-assertion adversarial isolation check against real HTTP
auth middleware, and real DNS + 3 HTTP probes against live production. No check was
weakened, no hook was bypassed, and no source was edited to obtain the pass.

## Evidence (independently reviewed, not just script-trusted)

1. **E2E full journey passes** — `tests/e2e/full-journey.test.js` boots the real
   spine + app on temp DBs (`:52-82`) and asserts each leg with data, not liveness:
   builder persist returns a `workflowId` (`:91-96`); the engine run returns
   `completed:true` and the summarize step's content (`:98-104`); console inventory
   contains the workflow (`:106-110`); run monitoring asserts
   `time_saved_minutes === 7` (`:112-128`); SOP markdown contains the workflow name
   (`:130-135`); ROI asserts `totalTimeSavedMinutes >= 7` (`:137-143`). Gate run:
   **6 pass / 0 fail / 1 skip** (`/tmp/p11-e2e.log`).

2. **The converger leg was verified, not assumed** — sub-test 7 (`:145-156`)
   self-skips without `ANTHROPIC_API_KEY`, so the gate run alone does **not** prove
   Done-when #1's "builder (converger)". I re-ran the suite with a real key
   (`node --env-file=.env --test tests/e2e/full-journey.test.js`) and it went
   **7 pass / 0 fail / 0 skip** — `ok 7 - builder: converger produces a runnable
   spec (real LLM)`, i.e. `runHeadless` returned a spec carrying a trigger and
   nodes. The converger path is genuinely exercised.

3. **Cross-tenant isolation, adversarially** — `scripts/checks/p11-cross-tenant-adversarial.mjs`
   boots the real spine on an ephemeral port with two tenants and drives the real
   auth middleware. **18/18 assertions passed** (`/tmp/p11-adv.log`,
   `CROSS-TENANT-ADVERSARIAL-PASS`), covering every resource class the Done-when
   names: workflows (B reading A's → 404 with no payload, and A absent from B's
   inventory — `:80-85`), runs (B → 404, `:90-91`), vault/OAuth tokens
   (cross-tenant `get` → `null`; **unscoped `get` throws** — `:94-98`), and RAG
   (physically separate DB files, `:106`). The store layer is re-proven fail-closed
   independently of HTTP: `userStore.list()` unscoped throws; `workflowStore.get`/
   `.getRun` under the wrong tenant return `null` (`:122-125`).

4. **The RAG check did not pass vacuously** — its query-level assertions are guarded
   by a positive control (`:111-119`) so they cannot pass on empty hit-lists when the
   embedding model is unavailable. In this run the control **fired**, so the real
   bidirectional assertions executed: `ok RAG: A retrieves its own doc but NOT B's`
   and `ok RAG: B retrieves its own doc but NOT A's`. Both directions confirmed.

5. **VPS runbook exists and is followable** — `docs/deployment/vps-runbook.md`, 11
   sections, each a concrete runnable command: hardening (`:13-19`), Node 22
   (`:21-26`), clone/`npm ci` (`:28-33`), `.env` incl. `OAUTH_REDIRECT_BASE` and the
   fail-fast `validateBootConfig()` note (`:35-62`), first-boot setup token
   (`:64-69`), the **systemd unit** (`:72-89`), **Cloudflare Tunnel + DNS**
   (`:94-107`), the smoke command that closes this gate (`:111-115`), off-host
   backups (`:117-123`), deploy/rollback (`:125-135`). It is followable in the
   strongest available sense: it describes the deployment that **actually exists** —
   `systemd` unit `atlas`, run user `atlas`, `/home/atlas/atlas`, Cloudflare Tunnel
   to `localhost:3000` — matching ENGINEERING-LOG.md's production section.

6. **DNS live + production smoke passes** — `scripts/checks/p11-smoke.mjs` against
   `PROD_HOST=atlas.agntic.co` (`/tmp/p11-smoke.log`):
   ```
   ok   DNS atlas.agntic.co resolves — 104.21.54.215, 172.67.142.215
   ok   GET /health → 200 — status 200
   ok   GET /setup/status → 200 — status 200
   ok   GET / → 200 — status 200
   target: https://atlas.agntic.co
   SMOKE-PASS
   ```
   `/health` is checked for `status === 'ok'`, not merely 200 (`:39`).

7. **The live host really is this codebase** — DNS resolves to Cloudflare edge IPs,
   not the origin `YOUR_SERVER_IP`; that is *correct* for a Cloudflare Tunnel (the
   origin IP is never published in DNS by design, which is why `EXPECTED_IP` is
   optional at `p11-smoke.mjs:31`). Origin identity was therefore established
   independently: `curl -s https://atlas.agntic.co/health` returns
   `{"status":"ok","uptime":22484,"version":"1.3.11","engine":"ok","auth":"ok","llm":"ready","rag":"ok"}`
   and the repo's `package.json` version is **1.3.11** — the live process serves this
   commit's code, with engine, auth, LLM and RAG all healthy.

## Residuals (non-blocking; noted, not fixed)

- **Runbook deploy step is stale relative to ENGINEERING-LOG.md.** `docs/deployment/vps-runbook.md:125-128`
  gives the manual path (`git pull && npm ci && sudo systemctl restart atlas`), while
  ENGINEERING-LOG.md now prescribes `./scripts/deploy.sh` (which also polls `/health` and prints
  a rollback SHA). The manual path still works, so this does not block the gate — but
  the runbook should point at `deploy.sh` / `docs/deployment/update-protocol.md`.
- **Runbook check is existence-only.** `scripts/gates/p11.sh:31-32` asserts only
  `[ -f "$RUNBOOK" ]`; "followable" is inherently human-judgment and was reviewed
  by hand above (evidence 5). This is a correct division of labour, not a defect.
