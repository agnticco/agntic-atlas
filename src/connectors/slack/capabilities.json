{
  "connector": "slack",
  "description": "Map of Slack functions the workflow engine can use. The AI/converger reads the AVAILABLE subset (implemented AND the client's token grants requiredScopes) so it only proposes actions that genuinely work. To add a capability: add an entry here, implement its handler in index.js, and flip `implemented` to true. Actions not listed are not proposable.",
  "actions": [
    {
      "id": "post_message",
      "label": "Post a message to a channel",
      "description": "Post a message to a public or private channel the bot can access.",
      "requiredScopes": ["chat:write"],
      "config": [
        { "key": "target", "type": "string", "required": true, "hint": "Channel ID (C…) or #name" },
        { "key": "body", "type": "string", "required": false, "hint": "Message text; omit to use the prior step output" },
        { "key": "username", "type": "string", "required": false, "hint": "Override the bot display name (requires chat:write.customize)" },
        { "key": "icon_emoji", "type": "string", "required": false, "hint": "Override the bot icon emoji e.g. :robot_face: (requires chat:write.customize)" }
      ],
      "returns": ["ts", "slackChannel"],
      "implemented": true
    },
    {
      "id": "post_dm",
      "label": "Send a direct message to a user",
      "description": "Open a DM with a user (by Slack user ID or email) and send them a message.",
      "requiredScopes": ["chat:write", "im:write"],
      "config": [
        { "key": "user", "type": "string", "required": true, "hint": "Slack user ID (U…) or email address" },
        { "key": "body", "type": "string", "required": false, "hint": "Message text; omit to use the prior step output" }
      ],
      "returns": ["ts", "channel"],
      "implemented": true
    },
    {
      "id": "reply_in_thread",
      "label": "Reply in a thread",
      "description": "Post a threaded reply under an existing message.",
      "requiredScopes": ["chat:write"],
      "config": [
        { "key": "target", "type": "string", "required": true, "hint": "Channel ID (C…) or #name" },
        { "key": "thread_ts", "type": "string", "required": true, "hint": "ts of the parent message to reply under" },
        { "key": "body", "type": "string", "required": false, "hint": "Reply text; omit to use the prior step output" }
      ],
      "returns": ["ts"],
      "implemented": true
    },
    {
      "id": "add_reaction",
      "label": "Add an emoji reaction to a message",
      "description": "React to a message with an emoji.",
      "requiredScopes": ["reactions:write"],
      "config": [
        { "key": "target", "type": "string", "required": true, "hint": "Channel ID (C…) or #name" },
        { "key": "timestamp", "type": "string", "required": true, "hint": "ts of the message to react to" },
        { "key": "emoji", "type": "string", "required": true, "hint": "Emoji name without colons, e.g. white_check_mark" }
      ],
      "returns": ["ok"],
      "implemented": true
    },
    {
      "id": "upload_file",
      "label": "Upload a file or snippet to a channel",
      "description": "Upload text content or a file to one or more channels.",
      "requiredScopes": ["files:write"],
      "config": [
        { "key": "target", "type": "string", "required": true, "hint": "Channel ID (C…) or #name" },
        { "key": "content", "type": "string", "required": true, "hint": "File text content or prior step output" },
        { "key": "title", "type": "string", "required": false, "hint": "File title shown in Slack" },
        { "key": "filename", "type": "string", "required": false, "hint": "Filename with extension, e.g. report.txt" }
      ],
      "returns": ["fileId"],
      "implemented": true
    },
    {
      "id": "create_channel",
      "label": "Create a Slack channel",
      "description": "Create a new public or private channel.",
      "requiredScopes": ["channels:manage"],
      "config": [
        { "key": "name", "type": "string", "required": true, "hint": "Channel name (lowercase, no spaces)" },
        { "key": "is_private", "type": "boolean", "required": false, "hint": "true to create a private channel" }
      ],
      "returns": ["channelId", "name"],
      "implemented": true
    },
    {
      "id": "invite_to_channel",
      "label": "Invite a user to a channel",
      "description": "Invite one or more users to a channel.",
      "requiredScopes": ["channels:write.invites"],
      "config": [
        { "key": "target", "type": "string", "required": true, "hint": "Channel ID (C…) or #name" },
        { "key": "users", "type": "string", "required": true, "hint": "Comma-separated Slack user IDs to invite" }
      ],
      "returns": ["ok"],
      "implemented": true
    },
    {
      "id": "set_channel_topic",
      "label": "Set a channel topic",
      "description": "Update the topic of a channel.",
      "requiredScopes": ["channels:write.topic"],
      "config": [
        { "key": "target", "type": "string", "required": true, "hint": "Channel ID (C…) or #name" },
        { "key": "topic", "type": "string", "required": true, "hint": "New topic text" }
      ],
      "returns": ["ok"],
      "implemented": true
    },
    {
      "id": "pin_message",
      "label": "Pin a message in a channel",
      "description": "Pin an existing message to a channel.",
      "requiredScopes": ["pins:write"],
      "config": [
        { "key": "target", "type": "string", "required": true, "hint": "Channel ID (C…) or #name" },
        { "key": "timestamp", "type": "string", "required": true, "hint": "ts of the message to pin" }
      ],
      "returns": ["ok"],
      "implemented": true
    },
    {
      "id": "set_reminder",
      "label": "Set a reminder for a user",
      "description": "Create a reminder for the authorized user. Requires a user OAuth token (xoxp-).",
      "tokenType": "user",
      "requiredScopes": ["reminders:write"],
      "config": [
        { "key": "text", "type": "string", "required": true, "hint": "Reminder text" },
        { "key": "time", "type": "string", "required": true, "hint": "When to send — unix timestamp or natural language e.g. 'in 30 minutes'" },
        { "key": "user", "type": "string", "required": false, "hint": "Slack user ID to remind (omit for the bot itself)" }
      ],
      "returns": ["reminderId"],
      "implemented": true
    },
    {
      "id": "lookup_user",
      "label": "Look up a user by email",
      "description": "Resolve a Slack user ID from an email address. Uses users.list scan (more reliable than users.lookupByEmail which has undocumented Slack API restrictions).",
      "requiredScopes": ["users:read"],
      "config": [
        { "key": "email", "type": "string", "required": true, "hint": "Email address to look up" }
      ],
      "returns": ["userId", "displayName"],
      "implemented": true
    },
    {
      "id": "search_messages",
      "label": "Search messages in Slack",
      "description": "Search messages across the workspace including private channels and DMs. Requires a user OAuth token (xoxp-).",
      "tokenType": "user",
      "requiredScopes": ["search:read"],
      "config": [
        { "key": "query", "type": "string", "required": true, "hint": "Search query, supports Slack modifiers e.g. in:#channel from:@user" },
        { "key": "limit", "type": "number", "required": false, "hint": "Max results to return (default 10)" }
      ],
      "returns": ["messages"],
      "implemented": true
    },
    {
      "id": "get_channel_history",
      "label": "Read recent messages from a channel",
      "description": "Fetch the most recent messages from any channel type: public (channels:history), private (groups:history), DMs (im:history), or group DMs (mpim:history).",
      "requiredScopes": ["channels:history"],
      "config": [
        { "key": "target", "type": "string", "required": true, "hint": "Channel ID (C…) or #name" },
        { "key": "limit", "type": "number", "required": false, "hint": "Number of messages to fetch (default 20, max 100)" },
        { "key": "oldest", "type": "string", "required": false, "hint": "Fetch messages after this unix timestamp" }
      ],
      "returns": ["messages"],
      "implemented": true
    },
    {
      "id": "list_channels",
      "label": "List channels in the workspace",
      "description": "Returns all channels (public and private) with their IDs and names. The AI uses this to discover valid channel targets before posting. Requires channels:read for public; groups:read for private.",
      "requiredScopes": ["channels:read"],
      "config": [
        { "key": "limit", "type": "number", "required": false, "hint": "Max channels to return (default 200)" },
        { "key": "exclude_archived", "type": "boolean", "required": false, "hint": "Exclude archived channels (default true)" }
      ],
      "returns": ["channels"],
      "implemented": true
    },
    {
      "id": "list_users",
      "label": "List workspace members",
      "description": "Returns workspace members with IDs, names, and emails. The AI uses this to discover user IDs for DMs or lookups.",
      "requiredScopes": ["users:read", "users:read.email"],
      "config": [
        { "key": "limit", "type": "number", "required": false, "hint": "Max users to return (default 200)" }
      ],
      "returns": ["users"],
      "implemented": true
    },
    {
      "id": "join_channel",
      "label": "Join a channel",
      "description": "Have the bot join a public channel (useful before posting without chat:write.public).",
      "requiredScopes": ["channels:join"],
      "config": [{ "key": "target", "type": "string", "required": true, "hint": "Channel ID (C…) or #name" }],
      "returns": ["channelId"],
      "implemented": true
    },
    {
      "id": "leave_channel",
      "label": "Leave a channel",
      "description": "Remove the bot from a channel.",
      "requiredScopes": ["channels:manage"],
      "config": [{ "key": "target", "type": "string", "required": true, "hint": "Channel ID (C…) or #name" }],
      "returns": ["channelId"],
      "implemented": true
    },
    {
      "id": "remove_reaction",
      "label": "Remove an emoji reaction from a message",
      "description": "Remove a previously added emoji reaction.",
      "requiredScopes": ["reactions:write"],
      "config": [
        { "key": "target",    "type": "string", "required": true, "hint": "Channel ID (C…) or #name" },
        { "key": "timestamp", "type": "string", "required": true, "hint": "ts of the message" },
        { "key": "emoji",     "type": "string", "required": true, "hint": "Emoji name without colons" }
      ],
      "returns": ["ok"],
      "implemented": true
    },
    {
      "id": "get_reactions",
      "label": "Get reactions on a message",
      "description": "Read all emoji reactions on a specific message (useful for trigger/condition logic).",
      "requiredScopes": ["reactions:read"],
      "config": [
        { "key": "target",    "type": "string", "required": true, "hint": "Channel ID (C…) or #name" },
        { "key": "timestamp", "type": "string", "required": true, "hint": "ts of the message" }
      ],
      "returns": ["reactions"],
      "implemented": true
    },
    {
      "id": "list_pins",
      "label": "List pinned items in a channel",
      "description": "Get all pinned messages and files in a channel.",
      "requiredScopes": ["pins:read"],
      "config": [{ "key": "target", "type": "string", "required": true, "hint": "Channel ID (C…) or #name" }],
      "returns": ["pins"],
      "implemented": true
    },
    {
      "id": "get_user_profile",
      "label": "Get a user's detailed profile",
      "description": "Fetch full profile: title, phone, avatar, email, custom fields.",
      "requiredScopes": ["users.profile:read"],
      "config": [{ "key": "user", "type": "string", "required": true, "hint": "Slack user ID (U…)" }],
      "returns": ["displayName", "realName", "title", "phone", "email"],
      "implemented": true
    },
    {
      "id": "list_usergroups",
      "label": "List user groups",
      "description": "Get all user groups (teams) in the workspace with their IDs and handles.",
      "requiredScopes": ["usergroups:read"],
      "config": [{ "key": "include_disabled", "type": "boolean", "required": false }],
      "returns": ["groups"],
      "implemented": true
    },
    {
      "id": "get_workspace_info",
      "label": "Get workspace info",
      "description": "Get the workspace name, domain, and ID.",
      "requiredScopes": ["team:read"],
      "config": [],
      "returns": ["name", "domain", "id"],
      "implemented": true
    },
    {
      "id": "list_files",
      "label": "List files in the workspace or a channel",
      "description": "Browse files shared in the workspace, optionally filtered to a channel.",
      "requiredScopes": ["files:read"],
      "config": [
        { "key": "target", "type": "string", "required": false, "hint": "Filter to a specific channel (optional)" },
        { "key": "limit",  "type": "number", "required": false, "hint": "Max results (default 20)" }
      ],
      "returns": ["files"],
      "implemented": true
    },
    {
      "id": "get_dnd_status",
      "label": "Check a user's Do Not Disturb status",
      "description": "Check whether a user's DND is active — useful before sending a DM.",
      "requiredScopes": ["dnd:read"],
      "config": [{ "key": "user", "type": "string", "required": true, "hint": "Slack user ID (U…)" }],
      "returns": ["dndEnabled", "nextStartTs", "nextEndTs"],
      "implemented": true
    },
    {
      "id": "list_emoji",
      "label": "List custom emoji",
      "description": "Get all custom emoji names in the workspace (useful for picking valid reaction names).",
      "requiredScopes": ["emoji:read"],
      "config": [],
      "returns": ["emoji", "count"],
      "implemented": true
    },
    {
      "id": "manage_usergroup",
      "label": "Create or update a user group",
      "description": "Create a new user group or update its members. Useful for managing teams.",
      "requiredScopes": ["usergroups:write"],
      "config": [
        { "key": "handle",  "type": "string", "required": true,  "hint": "Group handle (no @)" },
        { "key": "name",    "type": "string", "required": false },
        { "key": "users",   "type": "string", "required": false, "hint": "Comma-separated user IDs to set as members" }
      ],
      "returns": ["groupId"],
      "implemented": false
    },
    {
      "id": "list_bookmarks",
      "label": "List channel bookmarks",
      "description": "Get all bookmarks in a channel.",
      "requiredScopes": ["bookmarks:read"],
      "config": [{ "key": "target", "type": "string", "required": true }],
      "returns": ["bookmarks"],
      "implemented": false
    },
    {
      "id": "add_bookmark",
      "label": "Add a bookmark to a channel",
      "description": "Add a URL bookmark to a channel's bookmarks bar.",
      "requiredScopes": ["bookmarks:write"],
      "config": [
        { "key": "target", "type": "string", "required": true },
        { "key": "title",  "type": "string", "required": true },
        { "key": "link",   "type": "string", "required": true, "hint": "URL to bookmark" }
      ],
      "returns": ["bookmarkId"],
      "implemented": false
    },
    {
      "id": "read_canvas",
      "label": "Read a Slack canvas",
      "description": "Read the content of a Slack canvas document.",
      "requiredScopes": ["canvases:read"],
      "config": [{ "key": "canvas_id", "type": "string", "required": true }],
      "returns": ["content"],
      "implemented": false
    },
    {
      "id": "write_canvas",
      "label": "Create or update a Slack canvas",
      "description": "Create a new canvas or update an existing one.",
      "requiredScopes": ["canvases:write"],
      "config": [
        { "key": "title",   "type": "string",   "required": true },
        { "key": "content", "type": "textarea", "required": false },
        { "key": "canvas_id", "type": "string", "required": false, "hint": "Omit to create new" }
      ],
      "returns": ["canvasId"],
      "implemented": false
    },
    {
      "id": "search_files",
      "label": "Search files in Slack",
      "description": "Search files across the workspace. Requires a user OAuth token (xoxp-).",
      "tokenType": "user",
      "requiredScopes": ["search:read.files"],
      "config": [
        { "key": "query", "type": "string", "required": true },
        { "key": "limit", "type": "number", "required": false }
      ],
      "returns": ["files"],
      "implemented": true
    },
    {
      "id": "search_private",
      "label": "Search private/DM content",
      "description": "Search messages in private channels, DMs, and group DMs. Requires a user OAuth token (xoxp-).",
      "tokenType": "user",
      "requiredScopes": ["search:read.private", "search:read.im", "search:read.mpim"],
      "config": [
        { "key": "query", "type": "string", "required": true },
        { "key": "limit", "type": "number", "required": false }
      ],
      "returns": ["messages"],
      "implemented": true
    },
    {
      "id": "send_as_user",
      "label": "Send a message as the user",
      "description": "Post a message that appears FROM the authorized user's account — not from the bot. Requires a user OAuth token.",
      "tokenType": "user",
      "requiredScopes": ["chat:write"],
      "config": [
        { "key": "target", "type": "string",   "required": true,  "hint": "Channel ID (C…) or #name" },
        { "key": "body",   "type": "string",    "required": false }
      ],
      "returns": ["ts"],
      "implemented": true
    },
    {
      "id": "send_dm_as_user",
      "label": "Send a DM as the user",
      "description": "Send a direct message that appears under 'Direct Messages' (not 'Apps') in the recipient's sidebar. Requires a user OAuth token.",
      "tokenType": "user",
      "requiredScopes": ["im:write"],
      "config": [
        { "key": "user", "type": "string",   "required": true, "hint": "Slack user ID (U…) or email" },
        { "key": "body", "type": "textarea", "required": false }
      ],
      "returns": ["ts", "slackChannel"],
      "implemented": true
    },
    {
      "id": "list_reminders",
      "label": "List reminders",
      "description": "List all reminders for the authorized user. Requires a user OAuth token.",
      "tokenType": "user",
      "requiredScopes": ["reminders:read"],
      "config": [],
      "returns": ["reminders"],
      "implemented": true
    },
    {
      "id": "set_status",
      "label": "Set user status",
      "description": "Set the authorized user's status emoji and status text. Requires a user OAuth token.",
      "tokenType": "user",
      "requiredScopes": ["users.profile:write"],
      "config": [
        { "key": "status_text",       "type": "string", "required": true,  "hint": "e.g. In a meeting" },
        { "key": "status_emoji",      "type": "string", "required": false, "hint": "e.g. :calendar:" },
        { "key": "status_expiration", "type": "number", "required": false, "hint": "Unix timestamp when status clears (0 = never)" }
      ],
      "returns": ["status_text", "status_emoji"],
      "implemented": true
    },
    {
      "id": "set_dnd",
      "label": "Set Do Not Disturb",
      "description": "Enable DND snooze for a given number of minutes (0 to turn off). Requires a user OAuth token.",
      "tokenType": "user",
      "requiredScopes": ["dnd:write"],
      "config": [
        { "key": "num_minutes", "type": "number", "required": false, "hint": "Minutes to snooze; 0 turns DND off" }
      ],
      "returns": ["snoozed", "snoozeEndTs"],
      "implemented": true
    },
    {
      "id": "star_message",
      "label": "Star a message",
      "description": "Star a message on behalf of the authorized user. Requires a user OAuth token.",
      "tokenType": "user",
      "requiredScopes": ["stars:write"],
      "config": [
        { "key": "target",    "type": "string", "required": true },
        { "key": "timestamp", "type": "string", "required": true }
      ],
      "returns": ["ok"],
      "implemented": true
    },
    {
      "id": "list_stars",
      "label": "List starred items",
      "description": "List items starred by the authorized user. Requires a user OAuth token.",
      "tokenType": "user",
      "requiredScopes": ["stars:read"],
      "config": [
        { "key": "limit", "type": "number", "required": false }
      ],
      "returns": ["items"],
      "implemented": true
    },
    {
      "id": "post_group_dm",
      "label": "Send a group direct message",
      "description": "Open a multi-person DM (MPIM) and send a message.",
      "requiredScopes": ["mpim:write"],
      "config": [
        { "key": "users", "type": "string", "required": true, "hint": "Comma-separated Slack user IDs (2–8 people)" },
        { "key": "body", "type": "string", "required": false, "hint": "Message text; omit to use the prior step output" }
      ],
      "returns": ["ts", "channel"],
      "implemented": true
    }
  ]
}
